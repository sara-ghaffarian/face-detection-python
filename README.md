# face-detection-python
A simple face detection project using Python and OpenCv

This project can detect faces in images using Haar Cascade Classifier.  
It includes:
- `face_detect.py` → main Python script for face detection  
- `test.jpg` → sample image for testing  

## How to Run
1. Install requirements:
   ```bash
   pip install opencv-python
